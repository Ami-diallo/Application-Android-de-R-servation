<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a2905devoir3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.2905Devoir3">
        <activity android:name=".Profil"></activity>
        <activity android:name=".Horaire" />
        <activity android:name=".MonCours" />
        <activity android:name=".MesCours" />
        <activity android:name=".RechercheAvancee" />
        <activity android:name=".Recherche" />
        <activity android:name=".Favoris" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>